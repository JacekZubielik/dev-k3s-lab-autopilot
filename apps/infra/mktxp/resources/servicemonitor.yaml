---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: mktxp-exporter
  name: mktxp-exporter
  namespace: mktxp
spec:
  selector:
    matchLabels:
      app: mktxp-exporter
  endpoints:
  - port: metrics
    interval: 30s
  # namespaceSelector:
  #   any: true

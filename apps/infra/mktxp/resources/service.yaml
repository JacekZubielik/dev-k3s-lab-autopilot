---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mktxp-exporter
  name: mktxp-exporter
spec:
  type: ClusterIP
  selector:
    app: mktxp-exporter
  ports:
  - protocol: TCP
    name: metrics
    port: 49090
    targetPort: 49090

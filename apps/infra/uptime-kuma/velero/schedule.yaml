---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    argocd.argoproj.io/sync-wave: "2"
    argocd.argoproj.io/sync-options: Delete=false,Prune=false
  name: schedule-5-min-full-backup
  namespace: velero
spec:
  schedule: '0/5 * * * *'
  template:
    storageLocation: s3-minio
    hooks: {}
    includedNamespaces:
    - uptime-kuma
    includedResources:
    - 'pv'
    - 'pvc'
    includeClusterResources: true
    excludedNamespaces:
    - default
    - kube-system
    - minio
    metadata:
      labels:
        type: 'full'
        schedule: 'daily'
    ttl: 24h0m0s

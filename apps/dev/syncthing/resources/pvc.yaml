---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
  labels:
    longhornvolume: pvc-syncthing
  name: pv-syncthing
spec:
  # capacity:
  #   storage: 60Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-storage
  volumeName: syncthing-pv-volume

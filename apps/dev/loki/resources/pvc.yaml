---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-loki-backend-0 # pvc-loki-backend-0
spec:
  # dataSource:
  #   name: pvc-lms-config-backup
  #   kind: VolumeSnapshot
  #   apiGroup: snapshot.storage.k8s.io
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 12Gi
  volumeName: pv-loki-backend # must reference Persistent Volume
  storageClassName: longhorn

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-loki-write-0 # pvc-loki-write
spec:
  # dataSource:
  #   name: pvc-lms-config-backup
  #   kind: VolumeSnapshot
  #   apiGroup: snapshot.storage.k8s.io
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 12Gi
  volumeName: pv-loki-write # must reference Persistent Volume
  storageClassName: longhorn
